{
    "DT": {
        "_transfer(address,address,uint256)": [
            {
                "Expr": "super._transfer(from,to,amount)",
                "Trapdoor": "data.address2uintMapping(user) == 0"
            },
            {
                "Expr": "super._transfer(from,to,amount.sub(takeFee(user,amount,true,true)))",
                "Trapdoor": "data.address2uintMapping(user) == 0"
            },
            {
                "Expr": "super._transfer(from,to,amount.sub(takeFee(user,amount,true,true)))",
                "Trapdoor": "data.address2uintMapping(from) == 1"
            },
            {
                "Expr": "super._transfer(from,to,amount)",
                "Trapdoor": "from == owner() || to == owner() || data.address2uintMapping(from) == 3 || data.address2uintMapping(to) == 3"
            },
            {
                "Expr": "super._transfer(from,to,amount)",
                "Trapdoor": "require(bool,string)(data.address2uintMapping(from) != 1,the address is in black list)"
            }
        ]
    },
    "DPW": {},
    "IDT": {},
    "IDPW": {},
    "FLAG": true
}